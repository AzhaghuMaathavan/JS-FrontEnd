/*let num=3.7
console.log(Math.floor(num))
console.log(1000+(Math.floor(Math.random()*9999)))
*/

/*let a = 'hi'
let b = 'hello'
let c = 'hi hello'
let d =String('xyz')
let e =toString('abc')

console.log(`${a} is not ${b}`)
*/

/* 
    function isPrime(n) {
    if (typeof n !== 'number' || !Number.isInteger(n)) return false;
    if (n <= 1) return false;
    if (n <= 3) return true; 
    if (n % 2 === 0) return false; 
    for (let i = 3; i * i <= n; i += 2) {
        if (n % i === 0) return false;
    }
    return true;
}

const n = 50;
for (let i = 1; i <= n; i++) {
    if (isPrime(i)) {
        console.log(i);
    }
}
*/


